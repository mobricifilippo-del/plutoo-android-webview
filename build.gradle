// build.gradle (Project)
plugins {
    id 'com.android.application' version '8.7.0' apply false
    // se non hai moduli library non serve altro
}

tasks.register("clean", Delete) {
    delete(rootProject.buildDir)
}
