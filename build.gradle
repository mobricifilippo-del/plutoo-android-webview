plugins {
    // Dichiara il plugin Android e la sua versione a livello root
    id("com.android.application") version "8.5.2" apply false
}

tasks.register("clean", Delete) {
    delete(rootProject.buildDir)
}
