// File di livello progetto
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // nessun classpath speciale richiesto per AdMob
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
